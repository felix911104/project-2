<link rel="stylesheet" href="/css/survey.css">
<div id="outerContainer">
<script src="/js/survey.js"></script>

   
<h1>Making Connections</h1>
<p>The goal of networking is to build career connections. Developing connections begins with identifying what you have in common with those around you. This short survey will assist you in identifying the networking members with whom you have commonalities.</p>
{{#each question}}
     <form>
      <h4>{{this.question}}</h4>
      <label class="radio-inline">
        <input type="radio" class="quest"name="optradio" checked>{{this.choiceA}}
      </label>
      <label class="radio-inline">
        <input type="radio" name="optradio">{{choiceB}}
      </label>
      {{#if choiceC}}
      <label class="radio-inline">
        <input type="radio" name="optradio">{{choiceC}}
      </label>
      {{/if}}
      {{#if choiceD}}
      <label class="radio-inline">
        <input type="radio" name="optradio">{{choiceD}}
      </label>
      {{/if}}
      {{#if choiceE}}
      <label class="radio-inline">
        <input type="radio" name="optradio">{{choiceE}}
      </label>
      {{/if}}
      {{#if choiceF}}
      <label class="radio-inline">
      <input type="radio" name="optradio">{{choiceF}}
      </label>
      {{/if}}

    </form>
{{/each}}


</div>

<script src="http://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>